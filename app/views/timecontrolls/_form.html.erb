<%= form_for(@timecontroll) do |f| %>
  <% if @timecontroll.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@timecontroll.errors.count, "error") %> prohibited this timecontroll from being saved:</h2>

      <ul>
      <% @timecontroll.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start %><br>
    <%= f.text_field :start %>
  </div>
  <div class="field">
    <%= f.label :end %><br>
    <%= f.text_field :end %>
  </div>
  <div class="field">
    <%= f.label :gap %><br>
    <%= f.text_field :gap %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
